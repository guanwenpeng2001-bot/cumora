# Changelog

本 fork 基于 [yetone/cumora](https://github.com/yetone/cumora),track 本地自托管定制。上游变更见上游仓库。

## [Unreleased] — self-host customizations

### Added

- **MCP 连接器管理(五期,注册表 + BYOA 注入)**:新表 `mcp_connectors` + `agent_mcp_connectors`(迁移 0010;agent_id 同样无 FK,同 0009 注释)。设置页新增"连接器"标签:列表/新增/编辑/删除/启用开关(stdio=命令+参数+环境变量,http=URL+请求头)。AgentEditor 加"连接器"勾选块(与技能块同构;云端 agent 标注"MCP 支持将在后续版本提供"=六期预留)。BYOA 注入全部由 daemon 显式完成(secure 模式引擎不读用户配置):Claude secure 合并进 daemon 自建的 `--mcp-config` JSON(cumora bridge 条目永远赢名冲突)、Claude compat 写 `<home>/.mcp.json`、Codex secure 追加 `-c mcp_servers.<name>={…}`(TOML 转义 + 连字符键名规范化)、其他引擎跳过并在 daemon 日志注明。注册表载荷同款:`/computers/me/agents` 带 mcpConnectors(jsonb 聚合)。实测:stdio 连接器创建→勾选 gpt→registry SQL 载荷验证;Claude seedHome 容器内单跑写出 `.mcp.json`;生成器单测 6/6(validate/Claude 合并/桥接防遮蔽/Codex argv 转义)
- **Skill 管理(四期)**:公司级 skill 库(新表 `skills` + `agent_skills`,迁移 0009;注意 `agent_skills.agent_id` 无 FK——participants.id 只有部分唯一索引,孤儿清理由代码路径承担)。设置页新增"技能"标签(`src/desktop/SkillsTab.tsx`):列表(名称/描述/来源/文件数/删除),三种安装来源——SkillHub 搜索安装(SKILLHUB_URL 未配置时提示可配)、本地 hub(LOCAL_SKILLHUB_PATH,.env 或 server_settings 的 `local_skillhub_path` 键运行时配;本机挂 `~/.agents/skills` 只读进容器 `/skillhub`,见 gitignored 的 docker-compose.override.yml)、粘贴 SKILL.md。AgentEditor 创建/编辑页加"技能"勾选:managed 勾选即物化到该 agent 的 FUSE 工作区(`agent_workspace` 的 `skills/<name>/`,复用 CLI 同款校验器,取消勾选即清),wake prompt 的渐进索引自动拾取;BYOA 经 daemon 注入——`/computers/me/agents` 载荷带 skills(jsonb 聚合),`EnginePersona.skills` 新增,Claude/Cursor 引擎写原生 skills 目录(`.claude/skills`/`.cursor/skills`,`seedEngineSkills` 带路径防穿越校验 + 禁用即清),纯 AGENTS.md 引擎(Codex/Grok 等)在 persona 头文件追加名称+描述索引。实测:本地 hub 读出 27 个技能,导入 frontend-design 后勾选给拾遗(managed),工作区物化成功,发消息问她装了哪些 skill,回答 "frontend-design";BYOA 侧 seedEngineSkills 单跑验证(写入/嵌套/禁用清除),GPT 已卸任故 daemon 实跑未验
- **用量仪表盘(三期,设置页"用量"标签)**:配额卡下方新增完整统计(cc-switch 风格):总量卡片(tokens 按新增输入/输出/缓存创建/缓存命中分列 + 总请求数 + 总成本 + 缓存命中率)、自绘 SVG 多线趋势图(成本/输入/输出/缓存命中,小时/天粒度,不加依赖)、统计维度 tab(按智能体:头像+名字+token/成本/请求数/成功率,managed/BYOA 混排;按模型;按提供商——提供商从模型 id 推导,前缀中继优先)、请求日志表(时间/智能体/模型/输入/输出/成本/状态,分页)、5s 自动刷新可关、当天/本周/自定义时间范围。新 API(纯读 llm_calls,租户隔离):`GET /api/usage/{summary,trend,by-agent,by-model,by-provider,logs}` + `GET/PUT /api/usage/pricing`。迁移 0008 新增 `model_pricing` 表(单价 + source_url + 核对日期 + note;首启 seed 我们在用的模型:DeepSeek V4 官价、Kimi 订阅等效参考价、图像/ASR 按量类型置零注明;operator 可改,`cost.ts` 查价链在 env 覆盖与硬编码种子之间插入该表,30s 快照,改价不重启);订阅制模型只显示 token
- **agent 模型区二期(创建/编辑页)**:AgentEditor 的模型字段升级为目录下拉(数据来自 `GET /api/models/available` 的 text 桶,可手输自定义;BYOA 仍用该 Computer 引擎上报目录)。模型下方新增可折叠"高级设置"块(默认折叠):推理档位(选项跟随模型提供商:kimi/deepseek 系 low/high/max,openai 系 none~xhigh,空=跟随全局)、上下文窗口(空=跟随模型默认)、输出上限、思考链开关、降级路由列表(增删排序;留空=跟随全局大脑链)。存 `participants.model_config` JSONB;读取优先级 agent model_config → 全局设置 → env。turn.ts 主跳按 model_config 覆盖 effort/max_output_tokens/thinking(false 时整体省略 reasoning 字段)/contextWindow(压缩阈值与硬上限),并经统一降级引擎跑 agent 级链(显式链优先,否则跟随全局大脑链)。BYOA(非 cloud)agent 的参数块显示"由引擎管理"且不可编辑。实测:拾遗(managed)pin deepseek-v4-pro + high 后 llm_calls 账本出现 deepseek-v4-pro 的 agent-turn 记录(经 deepseek 平台组路由)
- **全局模型设置(一期,设置页"模型"标签)**:新增 `server_settings` 表(迁移 0007,含 `participants.model_config` JSONB 二期预留列);运行时解析 DB → env 兜底(30s 刷新,写即失效,改配置不重启;pods 经注入的 DATABASE_URL 自行跟随)。角色全覆盖:大脑/小脑/压缩(主模型 + 降级路由 + 推理档位/输出上限)+ 图像/语音(主模型 + 降级路由)+ 向量(单模型,不换空间)。统一降级引擎 `server/src/agents/fallback.ts`:402/429/5xx/断连降级、401/400 不降级;文本角色经 `withModelFallback`(llm.ts)逐跳重试,图像(dashscopeImageClient)/语音(transcribeAudio)降级链收编为运行时设置读取;向量不降级(向量空间不兼容,注释写明)。新 API:`GET/PUT /api/settings/models`(仅站点管理员)、`GET /api/models/available?refresh=1`(sub2api 实时 + BYOA 引擎目录 + 配置兜底,能力分桶,5min 缓存)。UI:MeView 新增"模型"标签(`src/desktop/ModelsTab.tsx`),中英双语
- **sub2api 分组模型修正(平台分组 × 套餐档解耦)**:sub2api 组改按平台分(kimi/deepseek/openai/grok,账号挂本平台组;openai 组是 subscription 类型的 quota 锚点,其余 standard);cumora 的 free/pro/max 改为每档按平台映射(`SUB2API_TIER_<TIER>_GROUP_<OPENAI|KIMI|DEEPSEEK|GROK>`,未配平台回落该档 openai 组→旧单值变量)。开通流程 `provisionUser` 变为收敛式多 key:allowed_groups 给全平台组、订阅只落 openai 主组、每平台组一把 key(已在正确组的 key 复用不轮换,值从存量存储恢复,恢复不了才重铸);`users.sub2api_api_key` 存 JSON map(存量裸串读作 openai key,免迁移)。`llm.ts` 调用时按模型所属平台选 key(各平台组 /v1/models 视图,5min 缓存,原生平台优先于 openai 转售;修复 SDK 方法 unbound 调用丢 `this` 的 bug);路由闸门从"需 admin key"放宽为"只需 base URL",agent pod 注入 remap 后的 `SUB2API_INTERNAL_URL` 自行做平台路由;orchestrator 注入 pod 的 OPENAI_API_KEY 取 openai 平台 key 而非原始 JSON。agent 镜像升 v4。实测:四平台 curl 全 200(k3/qwen-max/grok-4/deepseek-v4-pro),managed agent 拾遗端到端回复正常(k3 走 kimi 组,group_id=13)
- **ChatGPT Web 渠道经 sub2api 聚合**:本机 codex-chatgpt-web 桥接器(127.0.0.1:17841,驱动已登录的 ChatGPT 网页会话)作为 sub2api 的 OpenAI apikey 账号接入(base_url=http://host.docker.internal:17841,挂 max 组)。请求契约(经实测):模型用 `chatgpt-web/*` slug;body 需带 `client_metadata["x-codex-turn-metadata"]`(thread_id/turn_id/request_kind=turn/sandbox/workspaces)+ input 首条为 `<environment_context>`(cwd 须落在 metadata workspaces 内,sandbox_mode 与 metadata 一致)。端到端已验证:curl → sub2api 网关(用户 key 鉴权)→ 桥接器 → ChatGPT 网页真实回复。注意:账号 api_key 用的是 Codex access_token(会过期,过期只影响 native 直转与模型目录刷新,`chatgpt-web/*` 路径由桥接器本地浏览器会话驱动、不受影响;过期后从 ~/.codex/auth.json 重新读值更新账号 credentials)
- **sub2api 订阅聚合网关接入(多用户自动开通)**:compose 新增 `sub2api` 服务(从 fork [guanwenpeng2001-bot/sub2api](https://github.com/guanwenpeng2001-bot/sub2api) 构建,fork 补了 `POST /api/v1/admin/users/:id/api-keys` 管理端建 key 端点,开通流程不再依赖被 Turnstile 保护的 `/auth/*`);复用 cumora-postgres 里独立的 `sub2api` 库与 cumora-redis 逻辑库 1,宿主端口 8082。新注册/审批用户经 `server/src/sub2api.ts` `provisionUser` 四步(建用户→绑组→建 key→持久化)自动开通,tier→组映射由 `SUB2API_TIER_*_GROUP_ID` 配置;存量账号用 `server/src/scripts/provision-sub2api-user.ts` 手动开通(拒绝覆盖已有 key)。**注意:写入 key 前必须先在 sub2api 后台给组挂订阅账号,否则该用户 LLM 调用全断**(见 deploy/README.md)
- **全栈 Docker 化(一条命令起全部)**:仓库根新增 `docker-compose.yml`——db(pgvector/pgvector:pg16,复用外部卷 `cumora-pgdata`)、redis(复用 `cumora-redis-data`)、server(生产镜像,启动前串 `npm run migrate`;挂载容器可达的 kubeconfig 副本 `~/.kube/config-docker`,经 `host.docker.internal` + `tls-server-name: kubernetes` 保留完整 TLS 校验)、web(`deploy/web.Dockerfile` 多阶段构建 dist + nginx,发布 8080,反代 `/api`、`/ws` WebSocket、`/uploads` 到 server;DNS resolver 变量写法使 server 重建后无需 reload)。`deploy/README.md` 记录日常操作与已知限制
- **语音输入(语音转文字)**:聊天输入框新增麦克风按钮,MediaRecorder 录音后经服务端 `POST /api/audio/transcription` 转写,文字插入草稿(不自动发送)。ASR 走 DashScope 兼容模式(`input_audio` + chat/completions),主模型 + `OPENAI_AUDIO_FALLBACK_MODELS` 降级链。前端改动:`src/desktop/ChatPane.tsx`、`src/components/icons.tsx`、`src/api/client.ts`、双语 locale 文案。后端:`server/src/llm.ts`(`transcribeAudio`)、`server/src/api/router.ts`(新端点,鉴权 + 10MB 上限)
- **DashScope(阿里云百炼)图像通道**:`server/src/llm.ts` 新增 `getImageClient()` 与 `dashscopeImageClient`——双路由(qwen-image* 走同步 multimodal-generation,wan*/wanx* 走异步 text2image 任务轮询),支持 `OPENAI_IMAGE_FALLBACK_MODELS` 降级链,结果在 shim 内下载为 base64(绕开 fake-ip VPN DNS 导致的 SSRF 误伤)。`router.ts` 头像生成与 `cli.ts` `cumora image` 切换到该 client
- **可配置的 embedding 供应商**:`server/src/agents/embeddings.ts` 支持 `OPENAI_EMBED_BASE_URL` / `OPENAI_EMBED_API_KEY` / `OPENAI_EMBED_MODEL`,并显式传 `dimensions`(DashScope text-embedding-v4 可用)
- **orchestrator pod 环境注入**(`server/src/agents/runtime/orchestrator.ts`):pod 清单新增注入 `AGENT_RUNTIME_SECRET`、`REDIS_URL`、`DATABASE_URL`(localhost 改写为 host.docker.internal)、`NOVITA_API_KEY` / `NOVITA_BASE_URL`(per-agent `novita/<model>` 路由可用)、`OPENAI_MODEL`(此前缺失导致 pod 使用镜像默认模型名);`OPENAI_BASE_URL` 现在从环境变量读取而非仅 sub2api

### Fixed

- **sub2api 网关账号路由修正**(全部实测 200):k3 不可路由(/v1/responses 入站平台=openai,platform=kimi 的账号被平台相等性过滤)——新建 platform=openai 的 apikey 账号 `kimi(路由)`(base_url=api.kimi.com/coding/v1,k3 系恒等映射,free/pro/max 全组),原 kimi 账号(配额探测展示)不动;dashscope 报 "Upstream service temporarily unavailable"——百炼无 /responses 端点,账号 extra 设 `openai_responses_mode=force_chat_completions` + `openai_responses_supported=false`(Responses→Chat 翻译链接管,回包上游 id 为 chatcmpl-* 可证),另查明容器内经宿主 fake-ip VPN DNS 解析 aliyuncs.com 间歇性丢 A 记录只返回 AAAA → 容器无 IPv6 路由拨号失败,compose 给 sub2api 服务 pin 公共 DNS(223.5.5.5/119.29.29.29)并用 `extra_hosts` 补回 host.docker.internal(自定义 dns 会让内嵌 DNS 对该名 NXDOMAIN);grok-4 不可路由——同平台过滤(platform=grok 的 OAuth 账号不在 openai 池)+ xAI 从容器直连被重置/超时,新建 `grok(路由)` 账号(platform=openai,api_key 用 OAuth 账号的 access_token,经宿主 mihomo 代理 127.0.0.1:6789 → 容器内 host.docker.internal:6789,model_mapping 把 grok-4 映射到 grok-4.20-reasoning)
- **语音输入竞态与健壮性**(ChatPane.tsx):getUserMedia 权限弹窗挂起期间切换房间 → 已卸载组件上 `rec.start()`(麦克风常亮)——await 后检查 unmount 标志,已卸载则停轨返回;快速连点麦克风 → 两个 recorder、第一个流泄漏——加同步门闩 `startingRef`;stop→onstop 极短窗口内再点 → `InvalidStateError`——stop 分支立即置 transcribing 并清空 recorderRef;录音加 120s 硬上限(到点自动停止并提示,已录片段照常转写,新增双语 locale key `chat.voiceMaxDuration`);转写失败 pill 统一显示本地化 `chat.voiceFailed`,原始英文报错只进 console;错误 pill 的 4.5s 自动清除改为可追踪 timer(新错误先 clear 旧定时器)
- **`resolveAssetUrl` 遗漏点补齐**(打包 Electron 下 404):NotificationWindow 通知头像、RichInput @提及 chip 头像、DocumentEditor 贴图(image 扩展 renderHTML 时解析,文档内容仍存相对路径,refresh-url 流程不受影响)、admin ObservabilityPage agent 头像;另修 `//cdn...` protocol-relative URL 被误判为相对路径的问题
- **打包桌面端(app:// 源)相对资源 URL 全部 404**:服务端返回的 `avatar_url` / 附件 `url` 是相对路径(`/uploads/...`),在 Electron 打包版里解析到 `app://cumora` 自身。新增 `resolveAssetUrl`(`src/api/client.ts`,以 `/` 开头时前缀 `getServerOrigin()`,浏览器同源部署返回 '' 时原样透传),接入所有消费者:`useCachedAvatarSrc`(头像统一入口,`fetchedFrom` 一律存解析后的绝对形式)、`AttachmentCard`(图片/下载/ImageViewer)、markdown 图片渲染器、邮件附件下载、AgentEditor 头像预览、WorkspaceSettingsModal 成员头像、admin UsersPage/WaitlistPage、桌面与移动 composer 附件预览
- Docker 镜像内 shell 脚本 CRLF 行尾导致 `exec /usr/local/bin/agent-entrypoint failed`(`server/docker/*.sh` 转 LF;Windows clone 的已知坑)
- inbox-triage `max_output_tokens` 500 → 2000:思维链模型(如 deepseek vision-exp)的 reasoning 会吃光 500 预算导致空 JSON、分拣失败

### Notes

- 本地部署模型矩阵:大脑 Kimi k3(OPENAI_BASE_URL 指向 Kimi coding 端点)、小脑/压缩 DeepSeek(经 `novita/` 前缀路由)、图像 DashScope、embedding DashScope text-embedding-v4
- K8s 使用 Docker Desktop 内置集群(context `docker-desktop`)+ squat/generic-device-plugin 提供 `/dev/fuse`
- 数据库容器使用 `pgvector/pgvector:pg16`(语义记忆);记账修复后 pod 可直连数据库(单租户本机环境接受的隔离让步)
